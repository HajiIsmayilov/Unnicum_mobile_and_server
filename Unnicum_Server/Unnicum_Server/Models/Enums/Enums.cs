namespace Unnicum_Server.Models.Enums
{
    public enum UserStatus { Active, Banned }
    public enum AreaLevel { City, Country, Continent, WorldWide }
    public enum PostStatus { Active, Passive }
    public enum NotificationStatus { UnSeen, Seen }
    public enum MediaType { Image, Video }
}
